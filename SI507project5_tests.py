import unittest
from SI507project5_code import *

class Test_Request(unittest.TestCase):
	# def setUp(self):

		# self.events = get_data_cache("AA_cache_content.json")
		# self.inst = []
		# for inst in self.events['events']:
		#     self.inst.append(Assignment(inst))

	#  This test checks the import statments to make sure all the correct packages are used


	# def test_user_data(self):
	# 	self.assertRaises(NameError, client_id, "Make sure you imported the client_id and client_secret from you eventbrite_data.py file")










if __name__ == "__main__":
	unittest.main(verbosity=2)
